# Demo Application for the Talk "OpenRewrite in a nutshell"
"mvn clean install" creates a war file compatible with WildFly <26.
Solutions to the different live examples are stored in branches, eg. "solution1".
